import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
  cards: {
    textAlign: "center",
    height: "100%",
  },
  card: {
    minWidth: "50px",
    margin: "8px",
    width: "100%",
    height: "94%",
  },

  title: {
    marginLeft: "2px",
  },
}));
